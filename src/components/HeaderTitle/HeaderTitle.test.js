import React from 'react';
import HeaderTitle from './HeaderTitle';
import { render } from '@testing-library/react';

describe('HeaderTitle', () => {
  it('should display an h1 with text', () => {
    
  });
})
